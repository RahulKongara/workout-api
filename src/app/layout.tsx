import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'WorkoutAPI - Fitness Database API',
  description: 'Power your fitness app with comprehensive workout data via our REST API',
  keywords: ['workout api', 'fitness api', 'exercise database', 'workout data'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}